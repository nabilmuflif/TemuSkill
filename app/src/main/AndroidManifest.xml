<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TemuSkill"
        tools:targetApi="31">

        <activity android:name=".activities.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SelectServiceActivity" />
        <activity android:name=".activities.IntroActivity" />
        <activity android:name=".activities.ProviderOrderDetailActivity"/>
        <activity android:name=".activities.WelcomeActivity" />
        <activity android:name=".activities.RoleSelectionActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.MainActivity" android:exported="true"/>
        <activity android:name=".activities.NotificationActivity" />
        <activity android:name=".activities.AddServiceActivity" />
        <activity android:name=".activities.OrderConfirmationActivity" />
        <activity android:name=".activities.PaymentQrisActivity" />
        <activity android:name=".activities.PaymentSuccessActivity" />
        <activity android:name=".activities.OrderDetailActivity" />
        <activity android:name=".activities.RateOrderActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".activities.LanguageActivity" />
        <activity android:name=".activities.MyServicesActivity" />
        <activity android:name=".activities.AboutUsActivity" />
        <activity android:name=".activities.CancelOrderActivity"/>
        <activity android:name=".activities.MyReviewsActivity" />
        <activity android:name=".activities.VerificationActivity" />
        <activity android:name=".activities.AdminDashboardActivity" />
        <activity android:name=".activities.TalentDetailActivity" />
        <activity android:name=".activities.UserDetailActivity" />

    </application>

</manifest>